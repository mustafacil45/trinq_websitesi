# İletişim formu (/api/contact) — SendGrid
SENDGRID_API_KEY=
# Birden fazla alıcı: virgülle ayır (opsiyonel)
CONTACT_RECEIVER_EMAIL=
# SendGrid’de doğrulanmış gönderen
CONTACT_SENDER_EMAIL=

# Sadece `npm run dev`: SendGrid’e gitmeden formu test et (1 = açık)
# CONTACT_DRY_RUN=1
